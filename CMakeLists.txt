CMAKE_MINIMUM_REQUIRED(VERSION 3.5)

PROJECT(mq_apicall_exit_c)

INCLUDE_DIRECTORIES(/opt/mqm/inc)

SET(CMAKE_INCLUDE_CURRENT_DIR ON)

SET(SOURCE_FILES smsolog.c)

ADD_LIBRARY(smsolog SHARED ${SOURCE_FILES})